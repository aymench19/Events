{% extends 'base.html.twig' %}

{% block title %}Edit Event{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-8">
            <h1>Edit Event</h1>
            <hr>
            {{ include('event/_form.html.twig', {'button_label': 'Update Event'}) }}
            <a href="{{ path('app_event_index') }}" class="btn btn-secondary mt-3">Back to Events</a>
            <div class="mt-3">
                {{ include('event/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}
